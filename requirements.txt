# Core MCP dependencies
mcp[cli]>=1.0.0

# PDF processing dependencies
docling[cpu]>=2.0.0
docling-core>=2.0.0

# Data validation
pydantic>=2.0.0
pydantic_core>=2.0.0

# Additional dependencies that might be needed by docling
Pillow>=9.0.0
numpy>=1.21.0

torch==2.8.0+cpu
